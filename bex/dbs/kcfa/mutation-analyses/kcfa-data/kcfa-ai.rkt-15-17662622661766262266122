mutate: Mutating with position-swap : #<syntax:/Users/nhejduk/Research-Local/blgt-parent/gtp-benchmarks/benchmarks/kcfa/unified/ai.rkt:134:7 ((define new-states (for/list ((proc (in-set procs))) (match-define (Closure lam benv*) proc) (match-define (Lam _ formals call*) lam) (define bindings (map (alloc time*) formals)) (define benv** (benv-extend* benv* formals bindings)) (define store* (st...> -> #<syntax:/Users/nhejduk/Research-Local/blgt-parent/gtp-benchmarks/benchmarks/kcfa/unified/ai.rkt:134:7 ((list->set new-states) (define new-states (for/list ((proc (in-set procs))) (match-define (Closure lam benv*) proc) (match-define (Lam _ formals call*) lam) (define bindings (map (alloc time*) formals)) (define benv** (benv-extend* benv* formals bindin...>
#s(run-status "ai.rkt" 15 next syntax-error #f #f #f #f)
