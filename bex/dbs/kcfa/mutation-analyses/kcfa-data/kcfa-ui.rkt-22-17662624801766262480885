mutate: Mutating with begin-drop : #<syntax:Research-Local/blgt-parent/gtp-benchmarks/benchmarks/kcfa/unified/ui.rkt:114:3 (begin (define res (for/list (((i vs) (in-hash ms))) (format "~a:\n~a" i (string-join (for/list ((v (in-set vs))) (format "\t~S" v)) "\n")))) (string-join res "\n"))> -> #<syntax:Research-Local/blgt-parent/gtp-benchmarks/benchmarks/kcfa/unified/ui.rkt:114:3 (begin (define res (for/list (((i vs) (in-hash ms))) (format "~a:\n~a" i (string-join (for/list ((v (in-set vs))) (format "\t~S" v)) "\n")))))>
#s(run-status "ui.rkt" 22 format-mono-store syntax-error #f #f #f #f)
